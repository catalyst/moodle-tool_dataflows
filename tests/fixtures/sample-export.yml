name: 'Example Dataflow'
steps:
  read:
    name: read
    description: 'Read a value'
    type: \tool_dataflows\step\sql_reader
    config:
      sql: 'SELECT 1'
  debugging:
    name: debugging
    description: 'Output the value to debugging'
    depends_on: read
    type: \tool_dataflows\step\debugging
  write:
    name: write
    description: 'Write the value to a file'
    depends_on: debugging
    type: tool_dataflows\step\stream_writer
    config:
      streamname: /tmp/file
      format: json
